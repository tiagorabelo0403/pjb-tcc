apiVersion: v1
kind: Secret
metadata:
  name: pjb-sovereign-gateway-tls
type: kubernetes.io/tls
stringData:
  # REPLACE: PEM certificate chain para *.api.pjb.jus.br
  # Emitir via ICP-Brasil (domínio .jus.br exige AC credenciada) ou cert-manager com emissor ICP
  tls.crt: REPLACE_ME
  # REPLACE: PEM private key correspondente ao certificado acima
  tls.key: REPLACE_ME
