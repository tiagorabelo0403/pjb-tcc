package com.tcc.pjb.backend.model.dto.processual.recursal.automation;

public record RecursalAutomationWorkspaceChecklistItemView(
        String codigo,
        String descricao,
        boolean obrigatorio) {
}
