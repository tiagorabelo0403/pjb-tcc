package com.tcc.pjb.backend.service.processual.recursal.workspace;

import com.tcc.pjb.backend.core.processo.recursal.domain.foundation.RecursalDeadlineNotificationBlueprint;
import com.tcc.pjb.backend.model.dto.processual.recursal.automation.RecursalAutomationRequest;
import com.tcc.pjb.backend.model.dto.processual.recursal.automation.RecursalAutomationWorkspaceChecklistItemView;
import com.tcc.pjb.backend.model.dto.processual.recursal.automation.RecursalAutomationWorkspaceTrackView;
import java.util.List;

public final class RecursalDeadlineNotificationTrackFactory {

    private RecursalDeadlineNotificationTrackFactory() {
    }

    public static RecursalAutomationWorkspaceTrackView buildTrack(String recursoPrincipal,
                                                                  RecursalAutomationRequest request) {
        return new RecursalAutomationWorkspaceTrackView(
                "ALERTAS_PRAZO_NOTIFICACOES_RECURSAIS",
                "Alertas de prazo e notificações recursais",
                recursoPrincipal,
                RecursalDeadlineNotificationBlueprint.secoes(recursoPrincipal, request),
                RecursalDeadlineNotificationBlueprint.checklist(recursoPrincipal, request).entrySet().stream()
                        .map(entry -> new RecursalAutomationWorkspaceChecklistItemView(entry.getKey(), entry.getValue(), true))
                        .toList(),
                List.copyOf(RecursalDeadlineNotificationBlueprint.alertas(recursoPrincipal, request))
        );
    }
}
